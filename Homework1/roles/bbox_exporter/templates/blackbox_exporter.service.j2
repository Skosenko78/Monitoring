[Unit]
Description=SNMP Exporter Version {{ bbox_exporter_version }}
After=network-online.target

[Service]
User=prometheus
Group=prometheus
Type=simple
ExecStart={{ bbox_exporter_bin }} --config.file={{ bbox_exporter_conf_dir }}/blackbox.yml

[Install]
WantedBy=multi-user.target
